# Questão 14: Peça para o usuário digitar uma velocidade inicial (em m/s), uma posição inicial (em m) e um instante de tempo (em s) e 
# imprima a posição de um projétil nesse instante de tempo. Use a fórmula matemática: y(t) = y(0) + v(0)*t + (g*(t**2)/2) 
# Onde, g é a aceleração da gravidade (-10m/s²), y(t) é a posição final, y(0) é a posição inicial, v(0) é a velocidade inicial e t é o instante de tempo.


v_ini = int (input("Vamos fazer um cálculo de qual a posição de um projétil para o calculo físico: y(t) = y(0) + v(0)*t + (g*(t**2)/2) primeiro: \nQual o valor da velocidade em metros por segundo?"))
p_ini = int (input("Qual o valor da posição inicial em metros?"))
t_ini = int (input("Qual o valor do tempo em segundos?"))

#imprimir a posição de um projétil nesse instante de tempo:
# y(t) = y(0) + v(0)*t + (g*(t**2)/2) 

gravity = -10/(t_ini**2)
result_1 = p_ini + (v_ini*t_ini) + gravity*((t_ini*2)/2)

p_final = print (f"Vamos lá! \n O projétil no instante final estará em:", result_1, "metros.")
